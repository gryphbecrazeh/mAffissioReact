import React, { Component } from 'react';
// Attempt at building an URL directory, switch case to define which web page to render
class Director extends Component{
    render(){
        return(
            <p>Hello World</p>
        );    
    }
}
export default Director;